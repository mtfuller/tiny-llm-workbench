# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# dist is intentionally committed: it's embedded into the tlw binary via
# web/embed.go, so `go build`/`go test` work without requiring Node. Run
# `task web:build` after changing anything under web/src and commit the
# result.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
