// suggestedModels seeds model picker dropdowns with a well-known MLX repo id
// so they aren't empty before any model has been trained in TLW. Any MLX
// repo id works even if it's not listed here — this is just a starting
// suggestion, not a restriction.
export const suggestedModels = ['mlx-community/Qwen2.5-0.5B-Instruct-4bit']
